# @george/lexicon

Generated by `pnpm gen` from `../../lexicons`. Do not edit `src/` by hand.

Use `./lexicons` (schema validation) and `./types/*` (record types). The generated
`src/index.ts` XRPC client is unused — it references `com.atproto.repo.*` schemas we
don't vendor — so it is excluded from typechecking; XRPC calls go through `@atproto/api`.
